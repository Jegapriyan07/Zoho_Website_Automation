# Promotion registry

One `{slug}.json` file is written here when you run:

```bash
npm run promote -- --slug <page-slug>
```

Tracks which `output/` builds were copied into the reference library and when.
